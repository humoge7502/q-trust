# REG-24: use pip-compile or uv lock for reproducible builds (was lower-bound only)
torch>=2.5,<3.0
torch-geometric>=2.5
scipy>=1.18.1
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
numpy>=1.26
redis>=5.0.0
# Lockfile: planner/requirements.lock (generate via pip-compile)
