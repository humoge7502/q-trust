# REG-24: use pip-compile or uv lock for reproducible builds (was lower-bound only)
torch>=2.14.0,<3.0
torch-geometric>=2.5
scipy>=1.12
fastapi>=0.141.1
uvicorn>=0.29
pydantic>=2.6
numpy>=1.26
redis>=8.1.0
# Security floor (pip-audit 2026-09-19): anyio<4.14.2 carries
# CVE-2026-63374/64847/63349; pulled in transitively via fastapi/starlette.
anyio>=4.14.2
# Lockfile: planner/requirements.lock (generate via pip-compile)
