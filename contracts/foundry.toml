[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false

# Foundry >= 1.8 rewrites `new C()` inside test contracts into
# vm.deployCode("path:Name") cheatcode calls (dynamic test linking, new
# default). Halmos 0.3.3 cannot execute that cheatcode, which breaks the
# symbolic suite's setUp. Disable it until Halmos supports the rewrite.
dynamic_test_linking = false
test = "test"
# Exclude OpenZeppelin's own test files from compilation
deny_warnings = false
ignored_warnings_from = ["lib/openzeppelin-contracts"]

# Remap OpenZeppelin imports
remappings = [
    "forge-std/=lib/forge-std/src/",
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/"
]

[rpc_endpoints]
base_sepolia = "${QTRUST_BASE_SEPOLIA_RPC}"

[etherscan]
base_sepolia = { key = "${QTRUST_BASESCAN_API_KEY}", url = "https://api-sepolia.basescan.org/api" }

[fuzz]
runs = 1000
max_test_rejects = 65536

[invariant]
runs = 1000
depth = 100
fail_on_revert = true
