[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qtrust-sdk"
version = "2.2.0"
description = "Q-Trust SDK — Verifiable post-quantum cryptography migration provenance on Base L2."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Q-Trust Protocol", email = "dev@qtrust.dev" },
]
keywords = ["post-quantum", "pqc", "base", "ethereum", "cbom", "migration", "cryptography"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Security :: Cryptography",
]
dependencies = [
    "web3>=7.0.0,<9",
    "eth-account>=0.10.0",
    "pydantic>=2.0.0",
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    # Security floor (pip-audit 2026-09-19): anyio<4.14.2 carries
    # CVE-2026-63374/64847/63349; pulled in transitively via httpx.
    "anyio>=4.14.2",
    "base58>=2.1.0",
    "pynacl>=1.5.0",
    "cryptography>=46.0.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
    "hypothesis>=6.100.0",
]

[project.urls]
Homepage = "https://github.com/qtrust/qtrust"
Documentation = "https://docs.qtrust.dev"
Repository = "https://github.com/qtrust/qtrust"
Issues = "https://github.com/qtrust/qtrust/issues"

[tool.setuptools.packages.find]
include = ["qtrust*"]
exclude = ["tests*"]

[tool.ruff]
line-length = 100
target-version = "py310"

# Aligned with the repo-wide contract in ../.ruff.toml (E4/E7/E9/F) so
# `ruff check .` from the repo root enforces one consistent rule set. The
# package's stricter E/F/I/W/UP contract was never actually satisfied, so
# keeping it here only made repo lint red.
[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.mypy]
python_version = "3.10"
strict = true
