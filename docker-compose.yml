# Q-Trust backend docker-compose.
# Starts the API (including indexer and webhook fan-out), the Postgres store,
# Redis, and the AI planner microservice.
#
# Usage:
#   cp .env.example .env
#   # edit .env with your RPC URL, private key, and contract addresses
#   docker compose up -d

services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    image: qtrust-backend:2.2.0
    container_name: qtrust-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      - path: .env
        required: false
    environment:
      - NODE_ENV=production
      - PORT=3001
      - HOST=0.0.0.0
      - QTRUST_REDIS_URL=redis://:${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}@redis:6379
      - QTRUST_PG_URL=postgres://${POSTGRES_USER:-qtrust}:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}@postgres:5432/${POSTGRES_DB:-qtrust}
      - QTRUST_PLANNER_URL=http://planner:8000
      - QTRUST_PLANNER_API_KEY=${QTRUST_PLANNER_API_KEY:?QTRUST_PLANNER_API_KEY must be set in .env}
      - QTRUST_SCAN_ALLOWED_ROOTS=${QTRUST_SCAN_ALLOWED_ROOTS:?QTRUST_SCAN_ALLOWED_ROOTS must be set in .env}
      - QTRUST_BASE_SEPOLIA_RPC=${QTRUST_BASE_SEPOLIA_RPC:-https://sepolia.base.org}
    volumes:
      - ${QTRUST_SCAN_HOST_ROOT:-.}:${QTRUST_SCAN_CONTAINER_ROOT:-/workspace/projects}:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - qtrust-net

  postgres:
    image: postgres:16-alpine
    container_name: qtrust-postgres
    restart: unless-stopped
    ports:
      - "127.0.0.1:5433:5432"
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-qtrust}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      - POSTGRES_DB=${POSTGRES_DB:-qtrust}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./backend/src/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-qtrust} -d ${POSTGRES_DB:-qtrust}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - qtrust-net

  planner:
    build:
      context: ./planner
      dockerfile: Dockerfile
    image: qtrust-planner:2.2.0
    container_name: qtrust-planner
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - QTRUST_PLANNER_API_KEY=${QTRUST_PLANNER_API_KEY:?QTRUST_PLANNER_API_KEY must be set in .env}
      - QTRUST_REDIS_URL=redis://:${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}@redis:6379
      - QTRUST_MODEL_PATH=/app/model.pt
      - QTRUST_DEADLINES_PATH=/app/data/algorithms.json
      # GPU features (RL agent). Set to "auto" to use the A100 when present;
      # the planner falls back to CPU transparently when no device is exposed.
      - QTRUST_DEVICE=auto
    # NVIDIA GPU passthrough (requires nvidia-container-toolkit on the host).
    # `docker compose up planner` then exposes the A100; verify with:
    #   docker exec qtrust-planner nvidia-smi
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    networks:
      - qtrust-net

  redis:
    image: redis:7-alpine
    container_name: qtrust-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}
    command:
      [
        "redis-server",
        "--appendonly",
        "yes",
        "--maxmemory",
        "256mb",
        "--maxmemory-policy",
        "allkeys-lru",
        "--requirepass",
        "${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}",
      ]
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "redis-cli --no-auth-warning -a \"$$REDIS_PASSWORD\" ping | grep -q PONG",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - qtrust-net

  prometheus:
    image: prom/prometheus:v2.53.4
    container_name: qtrust-prometheus
    restart: unless-stopped
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./ops/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./ops/prometheus/alert_rules.yml:/etc/prometheus/alert_rules.yml:ro
      - prometheus-data:/prometheus
    depends_on:
      - api
      - alertmanager
    networks:
      - qtrust-net

  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: qtrust-alertmanager
    restart: unless-stopped
    ports:
      - "127.0.0.1:9093:9093"
    environment:
      # Substitute ${QTRUST_ALERT_WEBHOOK} inside the config template.
      - QTRUST_ALERT_WEBHOOK=${QTRUST_ALERT_WEBHOOK:-http://127.0.0.1:5001/alerts}
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        sed 's|$${QTRUST_ALERT_WEBHOOK}|'"$${QTRUST_ALERT_WEBHOOK}"'|g'
        /etc/alertmanager/alertmanager.yml.tpl > /etc/alertmanager/alertmanager.yml
        && exec /bin/alertmanager --config.file=/etc/alertmanager/alertmanager.yml --storage.path=/alertmanager
    volumes:
      - ./ops/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml.tpl:ro
      - alertmanager-data:/alertmanager
    networks:
      - qtrust-net

  grafana:
    image: grafana/grafana:11.4.0
    container_name: qtrust-grafana
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:?GRAFANA_PASSWORD must be set in .env}
    volumes:
      - ./ops/grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - qtrust-net

volumes:
  redis-data:
    driver: local
  postgres-data:
    driver: local
  prometheus-data:
    driver: local
  alertmanager-data:
    driver: local
  grafana-data:
    driver: local

networks:
  qtrust-net:
    driver: bridge